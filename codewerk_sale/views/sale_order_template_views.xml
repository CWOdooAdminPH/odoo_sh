<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="sale_order_template_view_form" model="ir.ui.view">
        <field name="name">sale.order.template.view.form</field>
        <field name="model">sale.order.template</field>
        <field name="type">form</field>
        <field name="inherit_id" ref="sale_management.sale_order_template_view_form"/>
        <field name="arch" type="xml">
            <field name="note" position="before">
                <label for="header_html" string="Order Header:" class="mt-3"/>
                <field name="header_html" nolabel="1" placeholder="Order Header HTML"/>
            </field>
            <field name="note" position="before">
                <label for="note" string="Sale Note:" class="mt-3"/>
            </field>
        </field>
    </record>
</odoo>
